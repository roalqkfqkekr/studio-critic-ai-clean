// 1. Copy this file to `src/firebaseConfig.js`.
// 2. Paste the Firebase web app config copied from Firebase Console.
// 3. Keep `src/firebaseConfig.js` local. It is ignored by .gitignore.
//
// This MVP only uses Firebase AI Logic with the Gemini Developer API.
// It does not require Firestore, Storage, Auth, Cloud Functions, Express,
// Render, or a separate backend server.

export const firebaseConfig = {
  apiKey: "YOUR_FIREBASE_WEB_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  appId: "YOUR_FIREBASE_APP_ID",
};
